# Schemas

These files are normative machine contracts. Human documentation explains semantics, but implementation and tests must validate against these schemas and versioned fixtures.

Schema URLs are placeholders until the public Draigara schema domain is configured. Publishing a schema at a URL makes immutability and cache behaviour part of the release process.
